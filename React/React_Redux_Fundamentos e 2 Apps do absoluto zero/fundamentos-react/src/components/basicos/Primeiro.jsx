export default function Primeiro() {
  const msg = "Seja bem vindo(a)";
  return (
    <div>
      <h2>Primeiro Componente</h2>
      <p>{msg}</p>
    </div>
  );
}

// Para exporta posso usar dessa forma também
// export default Primeiro
