/**
 * Antes da estrutura de dados utilizando arrays, é colocado os dados armazenados em um variável
 * mas dessa forma só podemos colocar um valor por variável sendo assim se torna mais interessane a utilização de array para armazenar mais valores.
 *
 */

// armazenamento ultizando variáveis.
let avgTempJan = 31.9;
let avgTemFev = 35.3;
let avgTemMar = 42;
let avgTemApr = 38;
let avgTemMay = 25.5;

console.log("Temperatura Fevereiro: " + avgTemFev);
